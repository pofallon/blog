# MDX Content Pipeline API Contracts

This directory contains the API contracts for the MDX content pipeline feature.

## Files

- `mdx-schema.json` - JSON Schema for frontmatter validation
- `mdx-types.ts` - TypeScript type definitions (reference)

## Usage

These contracts define the data structures used by the MDX content loader and renderer.
They serve as the source of truth for validation and type checking.
